# TwitchProxy IPA Injector - Requirements

# No external Python packages required for basic functionality
# Python 3.6+ standard library is sufficient

# Optional: For better IPA handling
# pyzipper>=0.3.6  # For password-protected ZIP files

# Optional: For Mach-O manipulation
# macholib>=1.14   # For Mach-O file parsing

# Required system tools (install separately):
# - optool: brew install optool
# - insert_dylib: brew install insert_dylib
